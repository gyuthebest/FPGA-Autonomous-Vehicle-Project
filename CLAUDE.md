# CLAUDE.md

## 1. 프로젝트 목적

이 프로젝트는 FPGA 기반 신뢰도 및 위험도 기반 자율주행 로직 연구 프로젝트다.

---

## 2. 기본 작업 원칙

기존 알고리즘을 임의로 변경하지 않는다.

파라미터, 임계값, 상태 인코딩, latency, valid 정렬 등을 변경할 경우
변경 이유와 영향을 먼저 설명하고 사용자 승인을 받아야 한다.

모든 작업은 다음 순서를 따른다.

1. 관련 파일을 먼저 읽는다.
2. 현재 구현과 의존 관계를 파악한다.
3. 변경 계획을 제시한다.
4. 최소 범위만 수정한다.
5. 기존 검증을 실행한다.
6. 변경 사항에 대한 추가 검증을 실행한다.
7. 결과를 PASS/FAIL과 근거로 보고한다.
8. 검증되지 않은 것을 완료되었다고 말하지 않는다.

작업 시작 전에 관련 파일을 먼저 읽는다.

현재 작업이 보드 검증, CARLA, golden model, RTL 중 하나와 관련된 경우
해당 영역의 문서와 구현 파일을 확인한 후 작업한다.

특히 HANDOFF.md는 현재 프로젝트 상태를 파악하기 위해 우선 확인한다.

실보드 검증과 관련된 작업은
verification_reports/BOARD_BRINGUP_RUNBOOK.md를 우선 확인한다.

---

## 3. 변경 범위 제한

사용자가 특정 파일이나 모듈을 수정하라고 지정한 경우,
지정되지 않은 소스 파일은 원칙적으로 수정하지 않는다.

다른 소스 파일의 수정이 필요하다고 판단되면:

1. 왜 필요한지 설명한다.
2. 수정 대상 파일을 명시한다.
3. 사용자 승인을 받은 후 수정한다.

단, 검증 과정에서 생성되는 로그, 리포트, 캐시, 임시 파일 등은
소스 코드 변경으로 간주하지 않는다.

기존에 정상 동작하는 코드를 단순히 더 깔끔하게 만들기 위해
불필요하게 리팩터링하지 않는다.

요청과 직접 관련되지 않은 코드나 모듈은 건드리지 않는다.

---

## 4. 임의 추론 금지

정보가 부족하거나 요구사항이 여러 가지로 해석될 수 있는 경우
임의로 가정을 선택하지 않는다.

가능한 해석과 각각의 영향을 설명하고,
사용자에게 확인이 필요한 경우 먼저 질문한다.

특히 다음 항목은 승인을 받고 수정해야 한다.

- 임계값
- 파라미터
- 상태 인코딩
- timeout
- latency
- valid 정렬
- bit width
- signed/unsigned
- scaling
- sentinel 값
- risk 판정 기준
- reliability 판정 기준
- 센서 물리값과 raw 값의 변환 기준

기존 알고리즘과 다른 동작을 구현해야 할 경우
기존 동작과 변경 동작의 차이를 먼저 설명한다.

---

## 5. Golden Model 규칙

CARLA_FPGA_PROJECT/pl_model.py는
RTL 구현의 golden reference model이다.

Golden model과 RTL의 비교는
"FPGA가 알고리즘을 정확하게 구현했는가"를 검증한다.

이는 "알고리즘 자체가 올바른가"를 검증하는 것과 다르다.

알고리즘의 타당성은 다음을 통해 독립적으로 판단한다.

- 물리 시나리오
- 안전 불변조건
- 시나리오 라벨
- 독립적인 검증
- 실제 주행 데이터

RTL과 golden model의 불일치가 발견되었을 때
golden model을 RTL에 맞추는 방식으로 문제를 해결해서는 안 된다.

먼저 다음을 구분한다.

1. RTL 구현 오류
2. Golden model 오류
3. 테스트 또는 시나리오 생성 오류
4. 보드/통신/환경 오류

어느 쪽이 의도된 동작인지 확인한 후 수정한다.

Golden model과 RTL의 결과가 다르다는 이유만으로
어느 한쪽을 임의로 수정하지 않는다.

---

## 6. 검증 원칙

코드 수정 완료만으로 검증이 완료되었다고 판단하지 않는다.

가능한 경우 다음 순서로 검증한다.

1. 정적 코드 확인
2. 관련 simulation
3. lint/build
4. 기존 verification
5. golden model 비교
6. 필요할 경우 실보드 검증
7. 결과 기록

기존에 PASS한 검증은 변경하지 않고 그대로 유지한다.

검증 실패를 숨기거나 테스트 조건을 변경하여 PASS를 만들어서는 안 된다.

검증 결과를 보고할 때는 반드시 실제 실행 결과를 기준으로 한다.

검증을 실제로 실행하지 않았다면 PASS라고 표현하지 않는다.

---

## 7. 검증 결과 보고 형식

검증을 수행했다고 보고할 때 다음 정보를 포함한다.

- 실행한 명령
- 검증 대상
- 입력 범위 또는 sample 수
- PASS/FAIL
- PASS 개수
- FAIL 개수
- 주요 오류 내용
- 생성된 로그/리포트 경로

예:

BAD:

"보드 검증이 정상적으로 완료되었습니다."

GOOD:

"board_smoke_test.py --frames 30 --warmup 20 실행.
30/30 PASS, 0 FAIL.
평균 UDP RTT 0.40 ms.
결과 로그: ..."

검증 결과를 추측하거나 예상 결과를 실제 결과처럼 표현하지 않는다.

검증하지 못한 항목은 반드시 "미검증"이라고 명시한다.

---

## 8. 실보드 작업 규칙

보드에 영향을 줄 수 있는 작업은 실행 전에
작업 목적과 영향을 확인한다.

bitstream/programming/bring-up 관련 작업은
verification_reports/BOARD_BRINGUP_RUNBOOK.md를 우선 참조한다.

보드 검증에서 기존에 PASS한 테스트를 변경하거나 우회하지 않는다.

실보드에서 실패한 경우,
테스트 자체를 수정해서 PASS를 만드는 방식으로 해결하지 않는다.

실패 원인을 다음 범주로 구분한다.

1. 환경 문제
2. 통신 문제
3. RTL 문제
4. Golden model 문제
5. 테스트/시나리오 문제

보드가 이미 정상적으로 동작하고 있는 경우
필요하지 않은 programming, reset, configuration 변경을 반복하지 않는다.

실보드 검증 결과는 가능한 경우 로그와 수치로 남긴다.

---

## 9. CARLA 검증 규칙

CARLA 기반 검증에서는 GUI를 손으로 조작하는 방식에 의존하지 않는다.

자동 검증이 가능한 경우
live_scenario_verifier.py 및 프로젝트에서 정의한 자동 검증 절차를 사용한다.

CARLA 센서 입력, UDP 통신, FPGA 출력, golden model 결과를
서로 구분하여 확인한다.

CARLA 시나리오의 물리적 의미와 FPGA 구현 결과를 혼동하지 않는다.

---

## 10. RTL 작업 규칙

SystemVerilog 수정 시 기존 인터페이스와 timing 관계를 우선 확인한다.

특히 다음 항목을 임의로 변경하지 않는다.

- module interface
- signal width
- signed/unsigned
- clock domain
- reset 방식
- valid 신호
- sample sequence
- pipeline latency
- register stage
- 상태 인코딩
- threshold
- scaling
- sentinel 값

파이프라인 레지스터 추가 또는 제거로 latency가 변경되는 경우
관련 valid 및 sample sequence 정렬을 함께 확인한다.

DSP, LUT, FF, BRAM 등의 자원 사용량과 timing에 영향을 줄 수 있는
구조적 변경은 변경 이유와 예상 영향을 설명한다.

기존 알고리즘의 수학적 의미를 변경하지 않고
단순히 합성 가능한 형태로 바꾸는 경우에도
결과가 동일한지 검증한다.

---

## 11. 데이터 및 Scaling 규칙

센서의 물리 단위와 RTL raw 값의 scaling을 혼동하지 않는다.

raw threshold를 변경하거나 물리값을 비교할 때는
해당 신호의 LSB 및 signed/unsigned 표현을 먼저 확인한다.

sentinel 값은 특별한 의미를 가지므로 임의로 변경하지 않는다.

특히 distance, approach_speed 등의 무표적 sentinel과
관련 판정 조건은 기존 구현을 먼저 확인한 후 수정한다.

---

## 12. 코드 수정 원칙

최소 범위만 수정한다.

문제를 해결하기 위해 필요한 코드만 변경한다.

기존 코드의 동작을 이해하지 않은 상태에서
대규모 리팩터링을 수행하지 않는다.

새로운 abstraction, helper module, parameter, state 등을 추가할 경우
기존 구현보다 필요한 이유가 명확해야 한다.

요청하지 않은 기능을 추가하지 않는다.

---

## 13. 작업 완료 조건

작업은 다음 조건을 만족해야 완료된 것으로 간주한다.

- 코드 수정 완료만으로 작업 완료라고 판단하지 않는다.
- 관련 lint/build/simulation/verification을 가능한 범위에서 수행한다.
- 기존 PASS 테스트의 결과가 악화되지 않았는지 확인한다.
- 필요한 경우 golden model과 RTL 결과를 비교한다.
- 실보드 작업인 경우 실제 보드 결과를 확인한다.
- 검증 결과를 수치와 로그 경로로 보고한다.
- 검증하지 못한 항목은 미검증으로 명시한다.
- 문제가 남아 있으면 "완료"가 아니라 "부분 완료"로 보고한다.
- 사용자 승인 없이 알고리즘 또는 핵심 파라미터를 변경하지 않는다.

---

## 14. 작업 보고 원칙

작업이 끝난 후 다음 내용을 명확하게 구분하여 보고한다.

### 변경 사항

실제로 수정한 파일과 주요 변경 내용을 기록한다.

### 검증 결과

실제로 실행한 검증과 그 결과를 기록한다.

### 미검증 항목

실행하지 못했거나 추가 확인이 필요한 항목을 기록한다.

### 발견된 문제

새롭게 발견된 오류, 위험 요소 또는 기존 문제를 기록한다.

### 다음 작업

현재 상태에서 가장 합리적인 다음 검증 또는 작업을 제시한다.

"완료"라는 표현은 실제 검증 결과가 충분히 확보된 경우에만 사용한다.