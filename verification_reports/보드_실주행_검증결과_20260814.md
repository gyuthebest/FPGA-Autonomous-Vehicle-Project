> **[정정 2026-08-14] 이 보고서의 참조 모델 대조 수치는 폐기됐다.**
> 모델이 틀린 것으로 확인됐다. 사유는 `HANDOFF.md` 3절.
> 전송 계층(응답률·sequence·지연) 수치와 오탐률 실측은 유효하다.

# 보드 실주행 검증 결과 — 2026-08-14

캡처 `pl_capture_20260814_212652.csv` / 3,472표본 / 3분 개루프 / 고장 미주입

---

## 한 줄 결론

**gyro 수정과 lux 클램프 수정이 실주행에서 확인됐다.**
정상 주행 오탐률이 11채널 중 **10채널에서 0.00%**가 됐고, 보드는 지터 없는
재생에서 **3,472프레임 전송 전수 정상**했다.

---

## 1. 이번 주행으로 무엇을 확인했나

이전 세션에서 "새 주행이 있어야만 검증 가능"으로 남겨둔 항목이 두 개였다.

| 항목 | 왜 재생으로는 안 됐나 | 결과 |
|---|---|---|
| lux 클램프 수정 | `sensor_noise.py`는 CARLA가 **보내는 값**을 바꾼다. 이미 상수로 굳은 옛 캡처를 재생해서는 효과가 안 나타난다 | **6.39% → 0.00%** |
| gyro 수정 실주행 오탐률 | 재생 캡처는 궤적이 고정 | **gyro 3축 전부 0.00%** |

---

## 2. 정상 주행 오탐률 (고장 미주입, 잡음 ON)

고장이 하나도 없는 주행이므로 NORMAL 이외는 전부 오탐이다.

| 채널 | 이전 (HANDOFF 개루프) | **이번** | 판정 |
|---|---|---|---|
| accel_x | 9.57% | **4.87%** | 개선, 잔존 |
| accel_y | 7.14% | **0.00%** | 해결 |
| accel_z | — | **0.12%** | 사실상 해결 |
| gyro_x | 0.44% | **0.00%** | 해결 |
| gyro_y | 2.63% | **0.00%** | 해결 |
| gyro_z | 2.87% | **0.00%** | 해결 |
| lux | 6.39% | **0.00%** | 해결 |
| distance | — | **0.00%** | |
| approach_speed | — | **0.00%** | |
| temperature | — | **0.00%** | |
| humidity | — | **0.00%** | |

**INVALID 오탐은 11채널 전부 0건.** 위험도 판정이 멈추는 구간이 없었다.

`noise` 검사기 확정 **0%** — `||` → `&&` 변경이 실주행에서 확인됐다.
이전에 오탐의 주범이던 "10창 부호 반전 7회 초과"가 더 이상 단독으로
DEGRADED를 만들지 않는다.

---

## 3. 관계식별 판정

| 관계식 | 채널 | 임계 | 원시위반 | **확정** | 잔차 p90 | 잔차 최대 |
|---|---|---|---|---|---|---|
| 3 | accel_x | 76 | 6.0% | **4.9%** | 42 | 131 |
| 4 | accel_y | 76 | 0.0% | 0.0% | 18 | 78 |
| 5 | accel_z | 76 | 1.0% | 0.1% | 10 | 346 |
| 6 | gyro_x | 7300 | 0.9% | **0.0%** | 4096 | 42366 |
| 7 | gyro_y | 7300 | 0.8% | **0.0%** | 4096 | 35458 |
| 8 | gyro_z | 7300 | 0.6% | **0.0%** | 4588 | 11220 |
| 17 | gyro_z | 120 | 0.1% | 0.0% | 49 | 134 |

관계식 6/7/8은 **원시 위반이 1% 미만이고 확정은 0%**다. 디바운스가 정상적으로
과도현상을 걸러내고 있다는 뜻이다. 격한 주행 캡처(183046)에서 확정
5.2/10.2/12.0%였던 것과 비교하면 이번 주행이 온건했던 영향도 있으므로,
격한 주행 재측정은 별도로 필요하다.

**남은 것은 관계식 3(accel_x) 하나다.** 속도를 2표본 차분해 가속도를
추정하는 방식이라 급가감속에서 실제 가속도와 벌어진다. 잔차 최대 131이
임계 76을 넘는다. 이건 **파라미터/수식 변경이라 승인 없이 손대지 않았다.**

---

## 4. 보드 대조

### 4-1. CARLA 물린 캡처 (지터 있음)

```
37,972건 중 불일치 7건 (0.018%)
발생 구간 seq 1566 ~ 1649
accel_z 4건, accel_x 3건
지터 상관: 불일치 7개 전부가 100 ms 초과 사건 10표본 이내 (최대 거리 8)
```

| 관측 | 값 |
|---|---|
| HUD 경고 | 3452건 **100.00% 일치** |
| MRM | 3452건 **100.00% 일치** |
| TD 잔여초 | 3452건 **100.00% 일치** |

TD 잔여초가 100% 일치한 것은 이전(±1초 오차)보다 나아진 결과다.

### 4-2. 지터 없는 재생 (확정 판정)

같은 캡처의 REG 벡터를 CARLA 없이 보드에 직접 재생했다.

```
전송 3472  응답 3472  timeout 0  sequence 불일치 0
왕복 지연  평균 0.68 ms  최소 0.34  최대 3.96  p95 1.24

결과: PASS — 전 프레임 응답, sequence 일치,
      전송이 깨끗하다.
```

**→ 4-1의 불일치 7건은 로직이 아니라 전송 지터가 원인이다.**

---

## 5. 전송 지터 (이번 주행의 유일한 실환경 문제)

| 지표 | 값 |
|---|---|
| 평균 송신 간격 | 51.84 ms (목표 50) |
| p95 | 53.42 ms |
| 최대 | 269.13 ms |
| **100 ms 초과** | **63회 (1.81%)** |

100 ms를 넘으면 PL의 `timeout_phase_cnt`가 raw timeout을 한 번 내고 디바운스
위상이 어긋난다. 원인은 호스트 쪽이다 — **AMD Radeon 840M 내장그래픽(4 GB
공유)** 에서 CARLA와 1280×720 카메라 센서를 함께 돌리는 부하다.

RTL이나 보드 문제가 아니다. 재생 시 왕복 지연이 평균 0.68 ms인 것이 근거다.

완화책(적용함): CARLA 서버를 `-quality-level=Low -ResX=640 -ResY=480`으로 기동.

---

## 6. 화면 종횡비 — 이상 없음

`out of device memory` 대응 조정이 화면 비율을 망가뜨렸는지 확인했다.

| 확인 | 결과 |
|---|---|
| pygame 창 | 1280×720 (16:9) |
| 카메라 센서 | `screen.get_width/height()` → 동일 |
| `main.py:560` `smoothscale` | **발동 안 함** (크기 동일) |
| 계기판 원형 게이지 | **정원 유지** |

증거 스크린샷: `verification_reports/aspect_screenshot.png`

**잠재 위험은 남아 있다(사용자 판단으로 수정하지 않음).**
주 모니터가 1536×960 = **16:10**인데 창은 16:9다. `smoothscale`은 종횡비를
보존하지 않으므로 **F11 전체화면이나 창 리사이즈를 하면 세로로 약 11%
늘어난다.** 카메라는 세션 시작 시 한 번만 생성되고 리사이즈 때 재생성되지
않기 때문이다.

→ **F11과 창 크기 조절을 쓰지 마라.**

---

## 7. 보드 복구 (재부팅 이후)

컴퓨터 재시작으로 PL configuration이 날아가 있었다.

| 단계 | 결과 |
|---|---|
| bitstream 프로그래밍 | PASS |
| AXI 스모크 (JTAG, seq 0xA5A50001) | PASS |
| ping 192.168.1.10 | TTL=255, 손실 0% |

---

## 8. 미검증 / 남은 것

| 항목 | 상태 |
|---|---|
| **distance:stuck 0.24 m/s** | REG 워드 패치 재생으로만 확인. **실제 CARLA 버튼은 아직 안 눌러봤다** |
| 관계식 3 (accel_x 4.87%) | 미승인. 속도 2표본 차분 방식의 한계 |
| 관계식 17 위상 불일치 | 격한 주행(183046)에서 RTL이 모델보다 3표본 먼저 확정. 어느 쪽이 사양대로인지 미판정 |
| yaw ±180° 되감기 | 관계식 8 잔차 최대 1억 2천만. 미승인 |
| 라이브 시나리오 75건 | 이번에 재실행 안 함 |
| 고장 검출률/지연 재측정 | 보드 기준 재측정 미실행 |
| 격한 주행 오탐률 재측정 | 이번 주행은 온건했다. 급선회 포함 재측정 필요 |

setup 여유가 **+0.068 ns**로 얇다. 통과했지만 로직을 더 얹으면 음수가 된다.

---

## 9. 재현 명령

```bash
# 보드 복구 (재부팅 후 필수)
C:\Xilinx\Vitis\2022.2\bin\xsct.bat sources_1\verification\program_and_bringup.tcl
python CARLA_FPGA_PROJECT\board_smoke_test.py --frames 30 --warmup 20

# 주행 캡처 (CARLA 먼저 -quality-level=Low 로 기동)
set FPGA_ENABLED=1 & set CARLA_MAP=Town04 & set PL_VERIFY_LOG=1
set CARLA_APPLY_FPGA=0 & set CARLA_RUN_SECONDS=180
python CARLA_FPGA_PROJECT\main.py

# 오탐률 정량 지표
python CARLA_FPGA_PROJECT\pl_capture_metrics.py <capture.csv> --skip 20

# 확정 판정 (지터 제거)
python CARLA_FPGA_PROJECT\board_smoke_test.py --replay <capture.csv>
```
